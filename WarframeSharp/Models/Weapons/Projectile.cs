using System.ComponentModel;

namespace WarframeSharp
{
    [DefaultValue(None)]
    public enum Projectile
    {
        Discharge,
        Hitscan,
        Projectile,
        Thrown,
        None
    }
}